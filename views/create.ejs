<%- include('header')%>
<div class="ui grid">
    <div class="three wide column">
        <a href="/posts?author=<%= user._id %>" class="avatar avatar-link"
           data-title="<%= user.name%> | <%=({m:'男',f:'女',x:'保密'})[user.gender]%>"
           data-content="<%=user.bio%>">
            <img src="/img/<%=user.avatar%>" alt="" class="avatar">
        </a>
    </div>

    <div class="ten wide column">
        <form action="" class="ui form segment piled" method="post">
            <div class="field required">
                <label for="">标题</label>
                <input type="text" name="title">
            </div>
            <div class="field required relative">
                <label for="">内容</label>
                <!--编辑器选择-->
                <div class="editor-list ui inline dropdown">
                    <div class="text">富文本编辑器</div>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div class="item" data-value="ueditor">富文本编辑器</div>
                        <div class="item" data-value="editormd">markdown编辑器</div>
                    </div>
                </div>
                <!--编辑器类别-->
                <input type="hidden" name="type" value="ueditor">
                <div id="createContainer"></div>

                <!--<div id="test">-->
                    <!--<textarea name="" id="" cols="30" rows="10"><p><em>这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试这是editormd的测试</em><br><img src="/img/upload_bfcbd69e5eed451e49542a444e27f4c2.png" alt="图片" title="图片">\r\n<pre><code>function a(){\r\n    alert(\'b\')\r\n}\r\n</code></pre></textarea>-->
                <!--</div>-->
            </div>
            <input type="submit" class="ui button" value="发布">
        </form>
    </div>
</div>
<!--模态框-->
<div class="ui mini test modal">
    <div class="header">
        Modal Title
    </div>
    <div class="image content">
        <div class="description">
            A description can appear on the right
        </div>
    </div>
    <div class="actions">
        <div class="ui button cancel">Cancel</div>
        <div class="ui button ok">OK</div>
    </div>
</div>
<!-- 配置文件 -->
<script type="text/javascript" src="/ueditor/ueditor.config.js"></script>
<!-- 编辑器源码文件 -->
<script type="text/javascript" src="/ueditor/ueditor.all.js"></script>

<script src="/editormd/editormd.js"></script>
<!--<script src="/editormd/lib/marked.min.js"></script>-->
<!--<script src="/editormd/lib/prettify.min.js"></script>-->
<!--<script src="/editormd/lib/raphael.min.js"></script>-->
<!--<script src="/editormd/lib/underscore.min.js"></script>-->
<!--<script src="/editormd/lib/sequence-diagram.min.js"></script>-->
<!--<script src="/editormd/lib/flowchart.min.js"></script>-->
<!--<script src="/editormd/lib/jquery.flowchart.min.js"></script>-->

<!-- 实例化编辑器 -->
<script type="text/javascript">
    //初始化ueditor（富文本编辑器）
    function initUeditor(containerId) {
        let html='          <!--loading-->\n' +
            '                    <div class="ui-loader-container">\n' +
            '                        <div class="ui active centered inline loader"></div>\n' +
            '                    </div>\n' +
            '                    <!-- 加载编辑器的容器 -->\n' +
            '                    <div id="ueditorContent" name="content"></div>';
        $('#'+containerId).html(html);
        let ue = UE.getEditor('ueditorContent',{initialFrameHeight:400});
        ue.ready(() => {
            $('.ui-loader-container').remove();
        });
    }
    initUeditor('createContainer');
    let testEditor;
    function initEditormd(containerId) {
        let html='     <div id="editormdContent">\n' +
            '                        <textarea class="editormd-markdown-textarea" name="markdown"></textarea>\n' +
            '                        <!-- 第二个隐藏文本域，用来构造生成的HTML代码，方便表单POST提交，这里的name可以任意取，后台接受时以这个name键为准 -->\n' +
            '                        <textarea class="editormd-html-textarea" name="content"></textarea>\n' +
            '                    </div>';
        $('#'+containerId).html(html);
        testEditor = editormd("editormdContent", {
            width: "100%",
            height: 500,
            path : '../editormd/lib/',
            markdown : '',
            codeFold : true,
            //syncScrolling : false,
            saveHTMLToTextarea : true,    // 保存 HTML 到 Textarea
            searchReplace : true,
            watch : false,                // 关闭实时预览
            htmlDecode : "style,script,iframe|on*",            // 开启 HTML 标签解析，为了安全性，默认不开启
            //toolbar  : false,             //关闭工具栏
            //previewCodeHighlight : false, // 关闭预览 HTML 的代码块高亮，默认开启
            emoji : true,
            taskList : true,
            tocm            : true,         // Using [TOCM]
            tex : true,                   // 开启科学公式TeX语言支持，默认关闭
            flowChart : true,             // 开启流程图支持，默认关闭
            sequenceDiagram : true,       // 开启时序/序列图支持，默认关闭,
            //dialogLockScreen : false,   // 设置弹出层对话框不锁屏，全局通用，默认为true
            //dialogShowMask : false,     // 设置弹出层对话框显示透明遮罩层，全局通用，默认为true
            //dialogDraggable : false,    // 设置弹出层对话框不可拖动，全局通用，默认为true
            //dialogMaskOpacity : 0.4,    // 设置透明遮罩层的透明度，全局通用，默认值为0.1
            //dialogMaskBgColor : "#000", // 设置透明遮罩层的背景颜色，全局通用，默认为#fff
            imageUpload : true,
            imageFormats : ["jpg", "jpeg", "gif", "png", "bmp", "webp"],
            imageUploadURL : "/posts/upload",
            toolbarIcons : function() {
                // Or return editormd.toolbarModes[name]; // full, simple, mini
                // Using "||" set icons align right.
                return ["undo", "redo", "|", "bold", "hr","|","link","image", "||","search", "clear","watch", "fullscreen", "preview", "info", "help",]
            },
            onload : function() {
            }
        });
    }


//let testEditor2 = editormd.markdownToHTML("test", {//注意：这里是上面DIV的id
//    htmlDecode: "style,script,iframe",
//    emoji: true,
//    taskList: true,
//    tex: true, // 默认不解析
//    flowChart: true, // 默认不解析
//    sequenceDiagram: true, // 默认不解析
//    codeFold: true
//});
$('.editor-list').dropdown({
    action:'select',
    onChange: function(value, text, $selectedItem) {
        $('.mini.modal')
            .modal({
                onDeny    : function(){
                    window.alert('Wait not yet!');
                },
                onApprove : function() {
                    window.alert('Approved!');
                }
            })
            .modal('show')
        ;
        $('input[name="type"]').val(value);
//        if(value==='ueditor'){
//            console.log(1)
//            testEditor.editor.remove();
//            initUeditor('createContainer');
//        }else if(value==='editormd'){
//            UE.getEditor('ueditorContent').destroy();
//            initEditormd('createContainer');
//        }
        // custom action
    }
});
</script>
<%- include('footer')%>
